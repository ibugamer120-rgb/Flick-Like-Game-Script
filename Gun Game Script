-- Services
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local Workspace = game:GetService("Workspace")

local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()
local CurrentCamera = Workspace.CurrentCamera

local TargetUiParent = CoreGui or LocalPlayer:FindFirstChildOfClass("PlayerGui")

-- Aesthetic Theme Palette
local BG_DARK = Color3.fromRGB(12, 12, 14)
local PANEL_BODY = Color3.fromRGB(18, 18, 22)
local SEPARATOR_LINE = Color3.fromRGB(28, 28, 34)
local TEXT_MAIN = Color3.fromRGB(245, 245, 245)
local TEXT_MUTED = Color3.fromRGB(140, 140, 148)
local ACCENT_MINT = Color3.fromRGB(0, 255, 150)
local CRITICAL_RED = Color3.fromRGB(255, 60, 60)
local TOGGLE_DARK = Color3.fromRGB(26, 26, 32)

-- Functional Runtime Flags
local AimbotEnabled = false
local TargetLockEnabled = false
local WallCheckEnabled = false
local TeamCheckEnabled = false 
local DoubleJumpEnabled = false
local InvisEnabled = false
local EspBoxesEnabled = false  
local EspTracersEnabled = false 
local BaseFovRadius = 100
local SmoothnessDivisor = 5 

-- HARD-LOCKED CONFIGURATIONS
local DoubleJumpPower = 40
local DEFAULT_ROBLOX_FOV = 70

-- Dynamic Keybind Configuration State
local AIMBOT_TOGGLE_KEY = Enum.KeyCode.O 
local IsBindingKey = false

-- Physics State Management
local canDoubleJump = false
local hasDoubleJumped = false
local menuVisible = true

-- UI Component Reference Handlers
local SyncAimbotToggleButton = nil
local SyncKeybindLabel = nil
local SyncAimbotToggleTextLabel = nil

----------------------------------------------------------------
-- UNIVERSAL TEAM DETECTION MATRIX
----------------------------------------------------------------
local function IsTeammate(player)
	if not TeamCheckEnabled then return false end
	
	-- Check 1: Standard Roblox Team Service fallback
	if player.Team == LocalPlayer.Team and not player.Neutral then
		return true
	end
	
	-- Check 2: Attribute-based engine assignments (Rivals Style)
	local myTeamAttr = LocalPlayer:GetAttribute("Team") or LocalPlayer:GetAttribute("TeamID") or (LocalPlayer.Character and LocalPlayer.Character:GetAttribute("Team"))
	local targetTeamAttr = player:GetAttribute("Team") or player:GetAttribute("TeamID") or (player.Character and player.Character:GetAttribute("Team"))
	
	if myTeamAttr and targetTeamAttr and myTeamAttr == targetTeamAttr then
		return true
	end
	
	-- Check 3: Folder structure assignments fallback
	local teamsFolder = Workspace:FindFirstChild("Teams") or game:GetService("ReplicatedStorage"):FindFirstChild("Teams")
	if teamsFolder then
		for _, teamFolder in ipairs(teamsFolder:GetChildren()) do
			if teamFolder:FindFirstChild(LocalPlayer.Name) and teamFolder:FindFirstChild(player.Name) then
				return true
			end
		end
	end
	
	return false
end

----------------------------------------------------------------
-- RUNTIME TARGETING ENGINE (SNIPER SCOPE ACCURACY OVERHAUL)
----------------------------------------------------------------

local FOVCircle = Drawing.new("Circle")
FOVCircle.Visible = false
FOVCircle.Color = ACCENT_MINT
FOVCircle.Thickness = 1.5
FOVCircle.NumSides = 64
FOVCircle.Radius = BaseFovRadius
FOVCircle.Filled = false
FOVCircle.Transparency = 0.6

local function IsVisible(targetPart, character)
	local origin = CurrentCamera.CFrame.Position
	local direction = targetPart.Position - origin
	
	local raycastParams = RaycastParams.new()
	raycastParams.FilterType = Enum.RaycastFilterType.Exclude
	raycastParams.FilterDescendantsInstances = {LocalPlayer.Character, character, CurrentCamera}
	raycastParams.IgnoreWater = true
	
	local raycastResult = Workspace:Raycast(origin, direction, raycastParams)
	return raycastResult == nil
end

local function IsTargetValid(character)
	if not character or not character:IsDescendantOf(Workspace) then return false end
	
	local humanoid = character:FindFirstChildOfClass("Humanoid")
	local head = character:FindFirstChild("Head")
	local hrp = character:FindFirstChild("HumanoidRootPart")
	
	if not humanoid or not head or not hrp then return false end
	if humanoid.Health <= 0.1 or humanoid:GetState() == Enum.HumanoidStateType.Dead then
		return false
	end
	
	return true
end

-- Helper math to get dynamic FOV size scaled to camera magnification
local function GetDynamicFovRadius()
	local currentFov = CurrentCamera.FieldOfView
	local fovAdjustment = math.tan(math.rad(currentFov / 2)) / math.tan(math.rad(DEFAULT_ROBLOX_FOV / 2))
	return BaseFovRadius * fovAdjustment
end

local function GetClosestTarget()
	local closestTarget = nil
	local maxDistance = GetDynamicFovRadius()

	for _, player in ipairs(Players:GetPlayers()) do
		if player ~= LocalPlayer and player.Character then
			if IsTeammate(player) then
				continue 
			end
			
			local character = player.Character
			if IsTargetValid(character) then
				local head = character.Head
				local screenPos, onScreen = CurrentCamera:WorldToViewportPoint(head.Position)
				
				if onScreen then
					local mousePos = Vector2.new(Mouse.X, Mouse.Y + 36)
					local distanceToMouse = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
					
					if distanceToMouse < maxDistance then
						if WallCheckEnabled then
							if IsVisible(head, character) then
								maxDistance = distanceToMouse
								closestTarget = head
							end
						else
							maxDistance = distanceToMouse
							closestTarget = head
						end
					end
				end
			end
		end
	end
	return closestTarget
end

-- Delta-Time dependent simulation loop
RunService.RenderStepped:Connect(function(deltaTime)
	local mousePos = Vector2.new(Mouse.X, Mouse.Y + 36)
	local dynamicRadius = GetDynamicFovRadius()
	
	FOVCircle.Position = mousePos
	FOVCircle.Radius = dynamicRadius
	FOVCircle.Visible = AimbotEnabled and menuVisible

	if AimbotEnabled then
		local targetPart = GetClosestTarget()
		if targetPart and targetPart.Parent and IsTargetValid(targetPart.Parent) then
			local targetCFrame = CFrame.new(CurrentCamera.CFrame.Position, targetPart.Position)
			if TargetLockEnabled then
				CurrentCamera.CFrame = targetCFrame
			else
				-- Dynamic scoping scalar deceleration profiles
				local scopeMultiplier = CurrentCamera.FieldOfView / DEFAULT_ROBLOX_FOV
				local targetTrackingSpeed = SmoothnessDivisor * (1 / math.clamp(scopeMultiplier, 0.1, 1))
				
				-- Smooth Exponential Interp Math: 1 - e^(-k * dt)
				local interpolationFactor = 1 - math.exp(-targetTrackingSpeed * deltaTime * 3)
				CurrentCamera.CFrame = CurrentCamera.CFrame:Lerp(targetCFrame, math.clamp(interpolationFactor, 0, 1))
			end
		end
	end
end)

----------------------------------------------------------------
-- OPTIMIZED ESP SYSTEM GRAPHICS ENGINE
----------------------------------------------------------------

local ActiveEspRegistry = {}

local function CreateEspProfile(player)
	if ActiveEspRegistry[player] then return end
	
	local Box = Drawing.new("Square")
	Box.Visible = false
	Box.Color = ACCENT_MINT
	Box.Thickness = 1.5
	Box.Filled = false
	Box.Transparency = 0.8
	
	local Tracer = Drawing.new("Line")
	Tracer.Visible = false
	Tracer.Color = TEXT_MAIN
	Tracer.Thickness = 1
	Tracer.Transparency = 0.6
	
	ActiveEspRegistry[player] = {Box = Box, Tracer = Tracer}
end

local function RemoveEspProfile(player)
	if ActiveEspRegistry[player] then
		ActiveEspRegistry[player].Box:Destroy()
		ActiveEspRegistry[player].Tracer:Destroy()
		ActiveEspRegistry[player] = nil
	end
end

for _, p in ipairs(Players:GetPlayers()) do
	if p ~= LocalPlayer then CreateEspProfile(p) end
end
Players.PlayerAdded:Connect(CreateEspProfile)
Players.PlayerRemoving:Connect(RemoveEspProfile)

RunService.RenderStepped:Connect(function()
	for player, assets in pairs(ActiveEspRegistry) do
		local char = player.Character
		local showBox = false
		local showTracer = false
		
		if IsTargetValid(char) and not IsTeammate(player) then
			local root = char.HumanoidRootPart
			local pos, onScreen = CurrentCamera:WorldToViewportPoint(root.Position)
			
			if onScreen then
				local camDistance = (CurrentCamera.CFrame.Position - root.Position).Magnitude
				local boxHeight = math.clamp((1000 / camDistance) * 3, 10, 200)
				local boxWidth = boxHeight * 0.7
				
				if EspBoxesEnabled then
					assets.Box.Size = Vector2.new(boxWidth, boxHeight)
					assets.Box.Position = Vector2.new(pos.X - boxWidth/2, pos.Y - boxHeight/2)
					showBox = true
				end
				
				if EspTracersEnabled then
					assets.Tracer.From = Vector2.new(CurrentCamera.ViewportSize.X / 2, CurrentCamera.ViewportSize.Y)
					assets.Tracer.To = Vector2.new(pos.X, pos.Y)
					showTracer = true
				end
			end
		end
		
		assets.Box.Visible = showBox
		assets.Tracer.Visible = showTracer
	end
end)

----------------------------------------------------------------
-- KINETIC DOUBLE JUMP ENGINE
----------------------------------------------------------------

UserInputService.JumpRequest:Connect(function()
	if not DoubleJumpEnabled then return end
	
	local myChar = LocalPlayer.Character
	local hrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
	local hum = myChar and myChar:FindFirstChild("Humanoid")
	
	if hrp and hum and hum.Health > 0 then
		if canDoubleJump and not hasDoubleJumped then
			hasDoubleJumped = true
			hrp.AssemblyLinearVelocity = Vector3.new(hrp.AssemblyLinearVelocity.X, DoubleJumpPower, hrp.AssemblyLinearVelocity.Z)
		end
	end
end)

local function setupStateTracker(character)
	local humanoid = character:WaitForChild("Humanoid", 5)
	if not humanoid then return end
	
	humanoid.StateChanged:Connect(function(oldState, newState)
		if not DoubleJumpEnabled then return end
		if newState == Enum.HumanoidStateType.Landed then
			canDoubleJump = false
			hasDoubleJumped = false
		elseif newState == Enum.HumanoidStateType.Freefall then
			task.wait(0.1)
			if humanoid:GetState() == Enum.HumanoidStateType.Freefall then
				canDoubleJump = true
			end
		end
	end)
end

if LocalPlayer.Character then setupStateTracker(LocalPlayer.Character) end
LocalPlayer.CharacterAdded:Connect(setupStateTracker)

local savedMaterials = {}
task.spawn(function()
	while task.wait(0.2) do
		local myChar = LocalPlayer.Character
		if myChar then
			for _, part in ipairs(myChar:GetDescendants()) do
				if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
					if InvisEnabled then
						if not savedMaterials[part] then
							savedMaterials[part] = {Transparency = part.Transparency, CanCollide = part.CanCollide}
						end
						part.Transparency = 1
						part.CanCollide = false
					else
						if savedMaterials[part] then
							part.Transparency = savedMaterials[part].Transparency
							part.CanCollide = savedMaterials[part].CanCollide
							savedMaterials[part] = nil
						end
					end
				elseif part:IsA("Decal") then
					part.Transparency = InvisEnabled and 1 or 0
				end
			end
		end
	end
end)

----------------------------------------------------------------
-- MODERN ANIMATED UI ENGINE CONSTRUCTOR
----------------------------------------------------------------

local BaseGui = Instance.new("ScreenGui")
BaseGui.Name = "WeProvideEngine"
BaseGui.ResetOnSpawn = false
BaseGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
BaseGui.Parent = TargetUiParent

local function addCorner(parent, px)
	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, px or 6)
	corner.Parent = parent
end

local function addStroke(parent, color, thick)
	local stroke = Instance.new("UIStroke")
	stroke.Color = color
	stroke.Thickness = thick or 1
	stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
	stroke.Parent = parent
	return stroke
end

local function createRipple(frame, touchX, touchY)
	local circle = Instance.new("Frame")
	circle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	circle.BackgroundTransparency = 0.8
	circle.AnchorPoint = Vector2.new(0.5, 0.5)
	circle.Size = UDim2.new(0, 0, 0, 0)
	circle.Position = UDim2.new(0, touchX - frame.AbsolutePosition.X, 0, touchY - frame.AbsolutePosition.Y)
	addCorner(circle, 100)
	circle.Parent = frame

	local maxSize = math.max(frame.AbsoluteSize.X, frame.AbsoluteSize.Y) * 1.5
	TweenService:Create(circle, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
		Size = UDim2.new(0, maxSize, 0, maxSize),
		BackgroundTransparency = 1
	}):Play()
	task.delay(0.4, function() circle:Destroy() end)
end

----------------------------------------------------------------
-- STAGE 1: INTRO ANIMATING LOADER
----------------------------------------------------------------

local LoadingFrame = Instance.new("Frame")
LoadingFrame.Name = "IntroLoader"
LoadingFrame.Size = UDim2.new(0, 380, 0, 220)
LoadingFrame.Position = UDim2.new(0.5, -190, 0.5, -110)
LoadingFrame.BackgroundColor3 = BG_DARK
LoadingFrame.Parent = BaseGui
addCorner(LoadingFrame, 12)
addStroke(LoadingFrame, SEPARATOR_LINE, 1.5)

local LoaderTitle = Instance.new("TextLabel")
LoaderTitle.Size = UDim2.new(1, 0, 0, 60)
LoaderTitle.BackgroundTransparency = 1
LoaderTitle.Text = "WeProvide"
LoaderTitle.TextColor3 = TEXT_MAIN
LoaderTitle.Font = Enum.Font.GothamBold
LoaderTitle.TextSize = 24
LoaderTitle.Parent = LoadingFrame

local LoaderSub = Instance.new("TextLabel")
LoaderSub.Size = UDim2.new(1, 0, 0, 20)
LoaderSub.Position = UDim2.new(0, 0, 0, 54)
LoaderSub.BackgroundTransparency = 1
LoaderSub.Text = "Synchronizing environment runtime libraries..."
LoaderSub.TextColor3 = ACCENT_MINT
LoaderSub.Font = Enum.Font.Code
LoaderSub.TextSize = 11
LoaderSub.Parent = LoadingFrame

local BarTrack = Instance.new("Frame")
BarTrack.Size = UDim2.new(0, 300, 0, 6)
BarTrack.Position = UDim2.new(0.5, -150, 0.7, 0)
BarTrack.BackgroundColor3 = TOGGLE_DARK
BarTrack.Parent = LoadingFrame
addCorner(BarTrack, 3)

local BarFill = Instance.new("Frame")
BarFill.Size = UDim2.new(0, 0, 1, 0)
BarFill.BackgroundColor3 = ACCENT_MINT
BarFill.Parent = BarTrack
addCorner(BarFill, 3)

----------------------------------------------------------------
-- STAGE 2: APPLICATION ENGINE INTERFACE
----------------------------------------------------------------

local MainPanel = Instance.new("Frame")
MainPanel.Name = "MainConsole"
MainPanel.Size = UDim2.new(0, 560, 0, 360)
MainPanel.Position = UDim2.new(0.5, -280, 0.5, -180)
MainPanel.BackgroundColor3 = BG_DARK
MainPanel.BackgroundTransparency = 1
MainPanel.Visible = false
MainPanel.Parent = BaseGui
addCorner(MainPanel, 12)
local panelStroke = addStroke(MainPanel, SEPARATOR_LINE, 1.5)

local dragging, dragStart, startPos
MainPanel.InputBegan:Connect(function(input)
	if (input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch) and UserInputService:GetFocusedTextBox() == nil then
		dragging = true
		dragStart = input.Position
		startPos = MainPanel.Position
		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then dragging = false end
		end)
	end
end)
MainPanel.InputChanged:Connect(function(input)
	if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
		local delta = input.Position - dragStart
		TweenService:Create(MainPanel, TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
		}):Play()
	end
end)

local Sidebar = Instance.new("Frame")
Sidebar.Size = UDim2.new(0, 150, 1, 0)
Sidebar.BackgroundColor3 = PANEL_BODY
Sidebar.ClipsDescendants = true
Sidebar.Parent = MainPanel
addCorner(Sidebar, 12)

local HubLabel = Instance.new("TextLabel")
HubLabel.Size = UDim2.new(1, 0, 0, 50)
HubLabel.BackgroundTransparency = 1
HubLabel.Text = "  WeProvide"
HubLabel.TextColor3 = ACCENT_MINT
HubLabel.Font = Enum.Font.GothamBold
HubLabel.TextSize = 16
HubLabel.TextXAlignment = Enum.TextXAlignment.Left
HubLabel.Parent = Sidebar

local TabContainer = Instance.new("Frame")
TabContainer.Size = UDim2.new(1, -10, 1, -60)
TabContainer.Position = UDim2.new(0, 5, 0, 50)
TabContainer.BackgroundTransparency = 1
TabContainer.Parent = Sidebar

local TabListLayout = Instance.new("UIListLayout")
TabListLayout.Padding = UDim.new(0, 4)
TabListLayout.Parent = TabContainer

local ContentWindow = Instance.new("Frame")
ContentWindow.Size = UDim2.new(1, -165, 1, -35)
ContentWindow.Position = UDim2.new(0, 155, 0, 10)
ContentWindow.BackgroundTransparency = 1
ContentWindow.ClipsDescendants = true
ContentWindow.Parent = MainPanel

local ConsoleBanner = Instance.new("TextLabel")
ConsoleBanner.Size = UDim2.new(1, -165, 0, 20)
ConsoleBanner.Position = UDim2.new(0, 155, 1, -25)
ConsoleBanner.BackgroundTransparency = 1
ConsoleBanner.Text = "Status: Operational | Base linked."
ConsoleBanner.TextColor3 = TEXT_MUTED
ConsoleBanner.Font = Enum.Font.Code
ConsoleBanner.TextSize = 10
ConsoleBanner.TextXAlignment = Enum.TextXAlignment.Left
ConsoleBanner.Parent = MainPanel

task.spawn(function()
	while true do
		TweenService:Create(ConsoleBanner, TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {TextTransparency = 0.4}):Play()
		task.wait(1)
		TweenService:Create(ConsoleBanner, TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {TextTransparency = 0}):Play()
		task.wait(1)
	end
end)

local function updateStatus(text, isAlert)
	ConsoleBanner.Text = "Status: " .. tostring(text)
	local targetColor = isAlert and CRITICAL_RED or ACCENT_MINT
	TweenService:Create(ConsoleBanner, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {TextColor3 = targetColor}):Play()
end

local tabs = {}
local function createTabWindow(tabName)
	local TabButton = Instance.new("TextButton")
	TabButton.Size = UDim2.new(1, 0, 0, 34)
	TabButton.BackgroundColor3 = TOGGLE_DARK
	TabButton.BackgroundTransparency = 1
	TabButton.Text = "  " .. tabName
	TabButton.TextColor3 = TEXT_MUTED
	TabButton.Font = Enum.Font.GothamMedium
	TabButton.TextSize = 12
	TabButton.TextXAlignment = Enum.TextXAlignment.Left
	TabButton.ClipsDescendants = true
	TabButton.Parent = TabContainer
	addCorner(TabButton, 6)

	local ScrollFrame = Instance.new("ScrollingFrame")
	ScrollFrame.Size = UDim2.new(1, 0, 1, 0)
	ScrollFrame.Position = UDim2.new(0, 30, 0, 0)
	ScrollFrame.BackgroundTransparency = 1
	ScrollFrame.Visible = false
	ScrollFrame.BorderSizePixel = 0
	ScrollFrame.ScrollBarThickness = 3
	ScrollFrame.ScrollBarImageColor3 = ACCENT_MINT
	ScrollFrame.Parent = ContentWindow

	local ListLayout = Instance.new("UIListLayout")
	ListLayout.Padding = UDim.new(0, 6)
	ListLayout.Parent = ScrollFrame

	ListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
		ScrollFrame.CanvasSize = UDim2.new(0, 0, 0, ListLayout.AbsoluteContentSize.Y + 10)
	end)

	TabButton.MouseEnter:Connect(function()
		if TabButton.BackgroundTransparency == 1 then
			TweenService:Create(TabButton, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {BackgroundTransparency = 0.9, TextColor3 = TEXT_MAIN}):Play()
		end
	end)

	TabButton.MouseLeave:Connect(function()
		if tabs[tabName].Active == false then
			TweenService:Create(TabButton, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {BackgroundTransparency = 1, TextColor3 = TEXT_MUTED}):Play()
		end
	end)

	TabButton.MouseButton1Click:Connect(function()
		createRipple(TabButton, Mouse.X, Mouse.Y)
		for tName, data in pairs(tabs) do
			data.Active = false
			data.Button.BackgroundTransparency = 1
			data.Button.TextColor3 = TEXT_MUTED
			if data.Window.Visible then
				TweenService:Create(data.Window, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Position = UDim2.new(0, -30, 0, 0), ScrollBarImageTransparency = 1}):Play()
				task.delay(0.2, function() data.Window.Visible = false end)
			end
		end
		
		tabs[tabName].Active = true
		TabButton.BackgroundTransparency = 0
		TabButton.TextColor3 = TEXT_MAIN
		
		ScrollFrame.Visible = true
		ScrollFrame.Position = UDim2.new(0, 30, 0, 0)
		ScrollFrame.ScrollBarImageTransparency = 0
		TweenService:Create(ScrollFrame, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Position = UDim2.new(0, 0, 0, 0)}):Play()
	end)

	tabs[tabName] = {Window = ScrollFrame, Button = TabButton, Active = false}
	return ScrollFrame
end

local CombatTab = createTabWindow("Combat Matrix")
local EngineTab = createTabWindow("Physical Engine")
local VisualsTab = createTabWindow("Visuals Matrix")
local SettingsTab = createTabWindow("Settings Config") -- Dynamic Settings Node Added

tabs["Combat Matrix"].Active = true
tabs["Combat Matrix"].Window.Visible = true
tabs["Combat Matrix"].Window.Position = UDim2.new(0, 0, 0, 0)
tabs["Combat Matrix"].Button.BackgroundTransparency = 0
tabs["Combat Matrix"].Button.TextColor3 = TEXT_MAIN

----------------------------------------------------------------
-- DYNAMIC VISUAL COMPONENT BUILDERS
----------------------------------------------------------------

local function AppendToggle(targetTab, text, default, callback)
	local Frame = Instance.new("Frame")
	Frame.Size = UDim2.new(1, -10, 0, 42)
	Frame.BackgroundColor3 = PANEL_BODY
	Frame.Parent = targetTab
	addCorner(Frame, 6)
	local rowStroke = addStroke(Frame, SEPARATOR_LINE, 1)

	local Label = Instance.new("TextLabel")
	Label.Size = UDim2.new(0.7, 0, 1, 0)
	Label.Position = UDim2.new(0, 12, 0, 0)
	Label.BackgroundTransparency = 1
	Label.Text = text
	Label.TextColor3 = TEXT_MAIN
	Label.Font = Enum.Font.GothamMedium
	Label.TextSize = 12
	Label.TextXAlignment = Enum.TextXAlignment.Left
	Label.Parent = Frame

	local Button = Instance.new("TextButton")
	Button.Size = UDim2.new(0, 50, 0, 24)
	Button.Position = UDim2.new(1, -62, 0.5, -12)
	Button.BackgroundColor3 = default and ACCENT_MINT or TOGGLE_DARK
	Button.Text = default and "ON" or "OFF"
	Button.Font = Enum.Font.GothamBold
	Button.TextSize = 10
	Button.TextColor3 = default and BG_DARK or TEXT_MAIN
	Button.ClipsDescendants = true
	Button.Parent = Frame
	addCorner(Button, 4)

	local current = default
	
	Frame.MouseEnter:Connect(function()
		TweenService:Create(rowStroke, TweenInfo.new(0.2), {Color = Color3.fromRGB(45, 45, 55)}):Play()
	end)
	Frame.MouseLeave:Connect(function()
		TweenService:Create(rowStroke, TweenInfo.new(0.2), {Color = SEPARATOR_LINE}):Play()
	end)

	Button.MouseButton1Click:Connect(function()
		createRipple(Button, Mouse.X, Mouse.Y)
		current = not current
		local targetBG = current and ACCENT_MINT or TOGGLE_DARK
		local targetText = current and BG_DARK or TEXT_MAIN
		
		TweenService:Create(Button, TweenInfo.new(0.25, Enum.EasingStyle.Quad), {BackgroundColor3 = targetBG, TextColor3 = targetText}):Play()
		Button.Text = current and "ON" or "OFF"
		callback(current)
	end)
	
	return Button, Label
end

local function AppendSlider(targetTab, text, min, max, default, callback)
	local Frame = Instance.new("Frame")
	Frame.Size = UDim2.new(1, -10, 0, 52)
	Frame.BackgroundColor3 = PANEL_BODY
	Frame.Parent = targetTab
	addCorner(Frame, 6)
	local rowStroke = addStroke(Frame, SEPARATOR_LINE, 1)

	local Label = Instance.new("TextLabel")
	Label.Size = UDim2.new(0.6, 0, 0, 26)
	Label.Position = UDim2.new(0, 12, 0, 4)
	Label.BackgroundTransparency = 1
	Label.Text = text
	Label.TextColor3 = TEXT_MAIN
	Label.Font = Enum.Font.GothamMedium
	Label.TextSize = 12
	Label.TextXAlignment = Enum.TextXAlignment.Left
	Label.Parent = Frame

	local ValueDisplay = Instance.new("TextLabel")
	ValueDisplay.Size = UDim2.new(0.3, 0, 0, 26)
	ValueDisplay.Position = UDim2.new(1, -18, 0, 4)
	ValueDisplay.BackgroundTransparency = 1
	ValueDisplay.Text = tostring(default)
	ValueDisplay.TextColor3 = ACCENT_MINT
	ValueDisplay.Font = Enum.Font.GothamBold
	ValueDisplay.TextSize = 12
	ValueDisplay.TextXAlignment = Enum.TextXAlignment.Right
	ValueDisplay.Parent = Frame

	local Track = Instance.new("Frame")
	Track.Size = UDim2.new(1, -24, 0, 4)
	Track.Position = UDim2.new(0, 12, 1, -12)
	Track.BackgroundColor3 = TOGGLE_DARK
	Track.Parent = Frame
	addCorner(Track, 2)

	local Fill = Instance.new("Frame")
	Fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
	Fill.BackgroundColor3 = ACCENT_MINT
	Fill.Parent = Track
	addCorner(Fill, 2)

	local Knob = Instance.new("TextButton")
	Knob.Size = UDim2.new(0, 12, 0, 12)
	Knob.Position = UDim2.new((default - min) / (max - min), -6, 0.5, -6)
	Knob.BackgroundColor3 = TEXT_MAIN
	Knob.Text = ""
	Knob.Parent = Track
	addCorner(Knob, 6)
	addStroke(Knob, BG_DARK, 1)

	local sliding = false
	
	Frame.MouseEnter:Connect(function()
		TweenService:Create(rowStroke, TweenInfo.new(0.2), {Color = Color3.fromRGB(45, 45, 55)}):Play()
	end)
	Frame.MouseLeave:Connect(function()
		TweenService:Create(rowStroke, TweenInfo.new(0.2), {Color = SEPARATOR_LINE}):Play()
	end)

	Knob.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 then 
			sliding = true 
			TweenService:Create(Knob, TweenInfo.new(0.1), {Size = UDim2.new(0, 14, 0, 14), Position = UDim2.new(Knob.Position.X.Scale, -7, 0.5, -7)}):Play()
		end
	end)
	
	UserInputService.InputChanged:Connect(function(input)
		if sliding and input.UserInputType == Enum.UserInputType.MouseMovement then
			local relativeX = input.Position.X - Track.AbsolutePosition.X
			local pct = math.clamp(relativeX / Track.AbsoluteSize.X, 0, 1)
			local outVal = math.floor(min + (max - min) * pct)
			
			Fill.Size = UDim2.new(pct, 0, 1, 0)
			Knob.Position = UDim2.new(pct, sliding and -7 or -6, 0.5, sliding and -7 or -6)
			ValueDisplay.Text = tostring(outVal)
			callback(outVal)
		end
	end)
	
	UserInputService.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 then 
			sliding = false 
			TweenService:Create(Knob, TweenInfo.new(0.1), {Size = UDim2.new(0, 12, 0, 12), Position = UDim2.new(Knob.Position.X.Scale, -6, 0.5, -6)}):Play()
		end
	end)
end

-- Specialty Interactive Keybind Registry System Frame Generator
local function AppendKeybindSelector(targetTab, text, defaultKey, callback)
	local Frame = Instance.new("Frame")
	Frame.Size = UDim2.new(1, -10, 0, 42)
	Frame.BackgroundColor3 = PANEL_BODY
	Frame.Parent = targetTab
	addCorner(Frame, 6)
	local rowStroke = addStroke(Frame, SEPARATOR_LINE, 1)

	local Label = Instance.new("TextLabel")
	Label.Size = UDim2.new(0.6, 0, 1, 0)
	Label.Position = UDim2.new(0, 12, 0, 0)
	Label.BackgroundTransparency = 1
	Label.Text = text
	Label.TextColor3 = TEXT_MAIN
	Label.Font = Enum.Font.GothamMedium
	Label.TextSize = 12
	Label.TextXAlignment = Enum.TextXAlignment.Left
	Label.Parent = Frame

	local BindButton = Instance.new("TextButton")
	BindButton.Size = UDim2.new(0, 80, 0, 24)
	BindButton.Position = UDim2.new(1, -92, 0.5, -12)
	BindButton.BackgroundColor3 = TOGGLE_DARK
	BindButton.Text = "[" .. defaultKey.Name .. "]"
	BindButton.Font = Enum.Font.Code
	BindButton.TextSize = 11
	BindButton.TextColor3 = ACCENT_MINT
	BindButton.ClipsDescendants = true
	BindButton.Parent = Frame
	addCorner(BindButton, 4)
	addStroke(BindButton, SEPARATOR_LINE, 1)

	Frame.MouseEnter:Connect(function()
		TweenService:Create(rowStroke, TweenInfo.new(0.2), {Color = Color3.fromRGB(45, 45, 55)}):Play()
	end)
	Frame.MouseLeave:Connect(function()
		TweenService:Create(rowStroke, TweenInfo.new(0.2), {Color = SEPARATOR_LINE}):Play()
	end)

	BindButton.MouseButton1Click:Connect(function()
		createRipple(BindButton, Mouse.X, Mouse.Y)
		if IsBindingKey then return end
		
		IsBindingKey = true
		BindButton.Text = "..."
		BindButton.TextColor3 = TEXT_MUTED
		updateStatus("Waiting for user hardware key input matrix...", false)
		
		local signalConnection
		signalConnection = UserInputService.InputBegan:Connect(function(inputObject)
			if inputObject.UserInputType == Enum.UserInputType.Keyboard then
				local selectedKey = inputObject.KeyCode
				if selectedKey ~= Enum.KeyCode.Escape and selectedKey ~= Enum.KeyCode.M then
					AIMBOT_TOGGLE_KEY = selectedKey
					BindButton.Text = "[" .. selectedKey.Name .. "]"
					BindButton.TextColor3 = ACCENT_MINT
					callback(selectedKey)
					updateStatus("Keybind configured to: " .. selectedKey.Name, false)
				else
					BindButton.Text = "[" .. AIMBOT_TOGGLE_KEY.Name .. "]"
					BindButton.TextColor3 = ACCENT_MINT
					updateStatus("Keybind assignment rejected or core locked.", true)
				end
				IsBindingKey = false
				signalConnection:Disconnect()
			end
		end)
	end)
	
	return BindButton
end

----------------------------------------------------------------
-- CORE ELEMENT INTERFACE REGISTRATION
----------------------------------------------------------------

local function SetAimbotState(newState)
	AimbotEnabled = newState
	updateStatus("Aimbot Tracking -> " .. (AimbotEnabled and "ON" or "OFF"), false)
	
	-- Reflect programmatic state change back onto the visual UI components
	if SyncAimbotToggleButton then
		local targetBG = AimbotEnabled and ACCENT_MINT or TOGGLE_DARK
		local targetText = AimbotEnabled and BG_DARK or TEXT_MAIN
		TweenService:Create(SyncAimbotToggleButton, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {BackgroundColor3 = targetBG, TextColor3 = targetText}):Play()
		SyncAimbotToggleButton.Text = AimbotEnabled and "ON" or "OFF"
	end
end

-- Combat Elements Matrix Tab Layout Registry
SyncAimbotToggleButton, SyncAimbotToggleTextLabel = AppendToggle(CombatTab, "Enable Master Aimbot ["..AIMBOT_TOGGLE_KEY.Name.."]", false, function(s)
	SetAimbotState(s)
end)

AppendToggle(CombatTab, "Universal Team Filter Check", false, function(s)
	TeamCheckEnabled = s
	updateStatus("Team Safeguard Filter -> " .. (s and "ACTIVE" or "OFF"), false)
end)

AppendToggle(CombatTab, "Instant Camera Snap", false, function(s)
	TargetLockEnabled = s
	updateStatus("Snap Mechanics Matrix -> Altered", false)
end)

AppendToggle(CombatTab, "Raycast Wall Filtering", false, function(s)
	WallCheckEnabled = s
	updateStatus("Wall Filter Check -> Vector Validated", false)
end)

AppendSlider(CombatTab, "Target Field-of-View Radius", 30, 500, 100, function(v)
	BaseFovRadius = v
	updateStatus("FOV Scanning Dimension -> " .. v .. "px", false)
end)

AppendSlider(CombatTab, "Tracking Lerp Speed", 1, 30, 5, function(v)
	SmoothnessDivisor = v
end)

-- Physical Engine Elements Array
AppendToggle(EngineTab, "Air Impulse Double Jump", false, function(s)
	DoubleJumpEnabled = s
	canDoubleJump = false
	hasDoubleJumped = false
	updateStatus("Double Jump Tracking Core -> " .. (s and "READY" or "OFF"), false)
end)

AppendToggle(EngineTab, "Local Invisible Mode Profile", false, function(s)
	InvisEnabled = s
	updateStatus("Anisotropic Profile State Change", false)
end)

-- Visual Elements Array
AppendToggle(VisualsTab, "Draw 2D Bounding Boxes", false, function(s)
	EspBoxesEnabled = s
	updateStatus("ESP bounding framework modified", false)
end)

AppendToggle(VisualsTab, "Render Screen Directional Tracers", false, function(s)
	EspTracersEnabled = s
	updateStatus("Vector line tracers updated", false)
end)

-- Settings Elements Array Setup
SyncKeybindLabel = AppendKeybindSelector(SettingsTab, "Aimbot Activation Keybind", AIMBOT_TOGGLE_KEY, function(newKey)
	if SyncAimbotToggleTextLabel then
		SyncAimbotToggleTextLabel.Text = "Enable Master Aimbot [" .. newKey.Name .. "]"
	end
end)

----------------------------------------------------------------
-- RUNTIME INTRO EXPANSION SEQUENCE
----------------------------------------------------------------

task.spawn(function()
	local diagnosticMessages = {
		"Analyzing network packet loops...",
		"Caching local character configuration nodes...",
		"Compiling high-speed UI render pathways...",
		"Binding anti-lag target calculation loops...",
		"System optimization sequence finalized."
	}
	
	for index, msg in ipairs(diagnosticMessages) do
		LoaderSub.Text = msg
		local targetPercentage = index / #diagnosticMessages
		TweenService:Create(BarFill, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = UDim2.new(targetPercentage, 0, 1, 0)}):Play()
		task.wait(0.5)
	end
	
	TweenService:Create(LoadingFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = UDim2.new(0, 0, 0, 0), BackgroundTransparency = 1}):Play()
	task.wait(0.3)
	
	LoaderTitle:Destroy()
	LoaderSub:Destroy()
	BarTrack:Destroy()
	LoadingFrame:Destroy()
	
	MainPanel.Visible = true
	MainPanel.Size = UDim2.new(0, 0, 0, 0)
	MainPanel.BackgroundTransparency = 0
	
	TweenService:Create(MainPanel, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
		Size = UDim2.new(0, 560, 0, 360)
	}):Play()
end)

----------------------------------------------------------------
-- SYSTEM INPUT HARDWARE KEYBIND INTERRUPT MANIFEST
----------------------------------------------------------------

UserInputService.InputBegan:Connect(function(input, gameProcessed)
	-- Prevents execution if the user is currently choosing a custom keybind or entering chat messages
	if IsBindingKey then return end
	if gameProcessed or UserInputService:GetFocusedTextBox() ~= nil then return end
	
	-- Handle Menu UI Visibility Toggle (Hotkey: M)
	if input.KeyCode == Enum.KeyCode.M then
		menuVisible = not menuVisible
		if menuVisible then
			MainPanel.Visible = true
			TweenService:Create(MainPanel, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.new(0, 560, 0, 360)}):Play()
		else
			TweenService:Create(MainPanel, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = UDim2.new(0, 560, 0, 0)}):Play()
			task.delay(0.2, function() if not menuVisible then MainPanel.Visible = false end end)
		end
		
	-- Handle Dynamic Runtime Aimbot Global Toggle
	elseif input.KeyCode == AIMBOT_TOGGLE_KEY then
		SetAimbotState(not AimbotEnabled)
	end
end)
